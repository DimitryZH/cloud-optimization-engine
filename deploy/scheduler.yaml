name: projects/PROJECT_ID/locations/us-central1/jobs/cloud-cost-engine-daily-scan
description: Trigger cloud-cost-engine /scan endpoint
schedule: "0 9 * * *"
timeZone: "America/Toronto"
httpTarget:
  uri: https://CLOUD_RUN_URL/scan
  httpMethod: GET
  oidcToken:
    serviceAccountEmail: cloud-scheduler-invoker-sa@PROJECT_ID.iam.gserviceaccount.com
    audience: https://CLOUD_RUN_URL
